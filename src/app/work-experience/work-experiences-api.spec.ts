import { WorkExperiencesAPI } from './work-experiences-api';

describe('WorkExperiencesAPI', () => {
  it('should create an instance', () => {
    expect(new WorkExperiencesAPI()).toBeTruthy();
  });
});
